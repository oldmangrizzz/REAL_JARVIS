'use strict'; const fs=require('fs'); const path=require('path'); const { resolveWorkspaceRoot } = require('./workspace-root'); function logEvent(event, options={}){ const workspaceRoot=resolveWorkspaceRoot({ workspace: options.workspace ?? event.workspaceRoot }); const logDir=path.join(workspaceRoot, 'logs'); const line=JSON.stringify({ ...event, ts: new Date().toISOString() }); try { fs.mkdirSync(logDir,{recursive:true}); fs.appendFileSync(path.join(logDir,'events.jsonl'), line+'\n'); } catch (error) { if (process.env.SKILL_DRIFT_DEBUG === '1') console.warn('[skill-drift-detector] failed to write event log:', error instanceof Error ? error.message : String(error)); } } module.exports = { logEvent };
