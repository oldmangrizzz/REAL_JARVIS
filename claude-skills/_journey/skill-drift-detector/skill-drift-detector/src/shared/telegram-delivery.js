'use strict'; async function sendTelegram(groupId, topicId, message, options = {}) { const token = options.token || process.env.TELEGRAM_BOT_TOKEN; if (!token) throw new Error('Missing TELEGRAM_BOT_TOKEN for Telegram delivery.'); if (!groupId || !topicId) throw new Error('Telegram delivery requires both a group ID and topic ID.'); const response = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ chat_id: String(groupId), message_thread_id: Number(topicId), text: String(message).slice(0, 4096), disable_notification: options.priority !== 'high' }) }); const body = await response.json().catch(() => ({})); if (!response.ok || body.ok === false) throw new Error(body.description || `Telegram send failed with ${response.status}`); return body; } module.exports = { sendTelegram };
