'use strict'; const fs=require('fs'); const os=require('os'); const path=require('path'); function isDirectory(v){try{return fs.statSync(v).isDirectory()}catch{return false}} function hasSkillsDir(r){return Boolean(r)&&isDirectory(path.join(r,'skills'))} function resolveWorkspaceRoot(options={}){const explicit=options.workspace?path.resolve(String(options.workspace)):null; const cwd=process.cwd(); for(const candidate of [explicit, process.env.OPENCLAW_WORKSPACE, hasSkillsDir(cwd)?cwd:null, path.join(os.homedir(),'claw')].filter(Boolean)){ if(hasSkillsDir(candidate)) return candidate;} return explicit ?? (process.env.OPENCLAW_WORKSPACE ? path.resolve(process.env.OPENCLAW_WORKSPACE) : path.resolve(process.cwd()));} function resolveSkillsDir(options={}){ if(options.skillsDir) return path.resolve(String(options.skillsDir)); if(process.env.SKILL_DRIFT_SKILLS_DIR) return path.resolve(process.env.SKILL_DRIFT_SKILLS_DIR); return path.join(resolveWorkspaceRoot(options), 'skills'); } module.exports = { resolveWorkspaceRoot, resolveSkillsDir };
